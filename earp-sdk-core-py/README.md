# EARP SDK Core

> v0.1.0.dev0

共享基础包，被所有 EARP SDK（capability、runtime、connector、plugin）依赖。

## 包含

| 模块 | 内容 |
|:-----|------|
| `earp_sdk_core.errors` | `ConnectorError`(6 码对齐 L2-03 §C.6) + `CapabilityError`(8 码对齐 L2-03 §8.4) |
