# Sessions 页面实施计划

> **For Hermes:** Phase 1 改状态模型 + 前端重写，Phase 2 加详情页 + 后台过期任务。

**Goal:** 重写 Sessions 页面为异常监控面板——5 种状态模型 + 异常优先展示 + 行展开摘要。

**Architecture:** 前端纯 HTML/CSS/JS，后端状态模型待 Phase 2 同步。

---

## 受影响文件

| 文件 | 变更 |
|---|---|
| `apps/earp-admin/pages/sessions.html` | 重写 |
| `apps/earp-admin/css/admin.css` | 新增状态标签 |

---

### Task 1: CSS — 状态标签样式

**Files:** Modify `apps/earp-admin/css/admin.css`（末尾追加）

```css
/* ── Session status tags ── */
.tag-active { background: var(--accent-light); color: var(--accent); }
.tag-completed { background: var(--green-bg); color: var(--green); }
.tag-failed { background: var(--red-bg); color: var(--red); }
.tag-denied { background: #fef3c7; color: #92400e; }
.tag-expired { background: var(--bg-surface); color: var(--text-quaternary); }

/* ── Session row expand ── */
.session-row { cursor: pointer; }
.session-row:hover { background: var(--bg-surface); }
.session-expand {
  display: none;
  background: var(--bg-surface);
  font-size: 0.75rem;
  color: var(--text-secondary);
  padding: 0.4rem 0.75rem;
}
.session-expand.visible { display: table-row; }
.session-expand td { border-bottom: 1px solid var(--border-subtle); }
.error-detail { font-family: 'JetBrains Mono', monospace; font-size: 0.72rem; color: var(--red); white-space: pre-wrap; }

/* ── Stats bar ── */
.stats-bar {
  display: flex; gap: 1.5rem; padding: 0.5rem 0.75rem;
  background: var(--bg-surface); border-radius: var(--radius-md);
  margin-bottom: 0.75rem; font-size: 0.81rem;
}
.stats-bar .stat-item { display: flex; align-items: center; gap: 0.35rem; }
.stats-bar .stat-count { font-weight: 600; }
```

---

### Task 2: sessions.html — 重写

**核心变更**：
- 状态筛选默认 `failed`
- 列表每行可点击展开摘要
- 异常概览栏
- 去掉 New Session 按钮
- 保留 `EARP.fetchJSON('/v1/sessions?...')` API 接线
- Demo fallback（5 种状态各一条）

---

### Task 3: 验证

- [ ] 默认显示 failed/denied/expired 异常条
- [ ] 行点击展开显示错误摘要
- [ ] 切换筛选器正常
- [ ] 正常 Session 显示执行摘要
- [ ] 分页正常
